// 1. 输入 `3 + 5` / `10 * 2`，输出结果
// 2. 支持 + - * /，做除的时候要判断**除数不能为 0**
// 3. 输入错误容错（输入字母要提示，清空缓冲区）
// 4. 循环可以一直计算，输入`q`退出程序
// 知识点：`char`判断、分支 if-else，scanf 同时读取数字和运算符。

#include <stdio.h>

int main(){
    int a, b;
    while (1) {

    }
}